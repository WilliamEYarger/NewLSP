<Window x:Class="NewLSP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewLSP.UserControls"
        mc:Ignorable="d"
        Title="New Learning Study Project"
        Height="800" Width="1200"        
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="MenuItem">
            <Setter Property="FontSize"  Value="20"  />
            <Setter Property="Margin" Value="5,0,5,0"/>
        </Style>
    </Window.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem x:Name="miHome" Header="HomePage" 
                      Click="miHome_Click" >

            </MenuItem>
            <MenuItem x:Name="miSubjectTree" Header="SubjectTreePage" 
                      Click="miSubjectTree_Click" >

            </MenuItem>
            <MenuItem x:Name="miCreateQA" Header="Create/EditQAPage" 
                      Click="miCreateQA_Click">

            </MenuItem>
            <MenuItem x:Name="miTest" Header="Test/ReviewPage" Click="miTest_Click">

            </MenuItem>
        </Menu>

        <Grid Grid.Row="1">
            <local:Home x:Name="ucHome"></local:Home>
            <local:SubjectTree x:Name="ucSubjectTree" Visibility="Collapsed"/>
            <local:CreatEditQA x:Name="ucCreatEditQA" Visibility="Collapsed"/>
            <local:TestReview x:Name="ucTestReview" Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Window>
<!--<Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="25,2,25,2"  />
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="35"  />
        </Style>
       
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0"  BorderThickness="2" BorderBrush="Black"
                Width="1180" Height="50" 
                Padding="2,2,2,2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="btnHomePage" Content="Home Page" 
                       />
                
                <Button x:Name="btnTreePage" Content="Subject Tree Page" 
                         />
                
                <Button x:Name="btnQAPage" 
                         Content="Create/Edit QA Page"
                         />
                
                <Button x:Name="btnResultsPage" Content="Results Page" 
                        />
                
            </StackPanel>
            
        </Border>


        <ContentControl
            x:Name="UserContentControl"            
            Height="690"/>
    </Grid>-->
<!--</Window>-->
